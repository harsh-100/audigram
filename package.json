{
  "name": "audigram",
  "version": "1.0.0",
  "description": "A social platform for sharing audio content",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-deps": "concurrently \"cd backend && npm install\" \"cd frontend && npm install\"",
    "build": "concurrently \"cd backend && npm run build\" \"cd frontend && npm run build\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 